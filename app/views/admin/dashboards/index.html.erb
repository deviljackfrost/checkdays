
<body>
<h1>全ユーザー一覧</h1>
<div1>
  <% @users.each do |user| %>
    <content>
      <%= user.name %> - <%= user.email %>
      <br>
      <%= link_to '削除', admin_user_path(user), method: :delete, data: { confirm: '本当に削除しますか？' } %> 
    </content>
  <% end %>
</body>
</div1>